#include <H3D/RenderMetaData.h>

using namespace H3D;

/************************************************************************/
/*                      Renderbucket definitions                        */
/************************************************************************/

H3D::RenderBucket::RenderBucket() : renderables(0), renderState(0), vertexAttributeLayout(0), numVBOs(0)
{
}
